<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.google.android.apps.nexuslauncher" />
        <package android:name="com.google.android.apps.wallpaper" />
    </queries>

    <application
        android:name=".PixelLauncherEvolvedApp"
        android:allowBackup="false"
        android:description="@string/module_description"
        android:hasCode="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelLauncherEvolved">

        <!--
            No launcher category, so this does not appear in the app drawer.
            Every setting is in the launcher's own Home settings and the blur
            switch is in Wallpaper & Style, so an icon here would open a screen
            with nothing to change. The activity itself stays, exported and
            addressable by name, for a framework or a person that wants to ask
            whether the module was accepted.
        -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PixelLauncherEvolved">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Double tap is recognised by the launcher; this root-backed endpoint locks it. -->
        <provider
            android:name=".lock.ScreenLockProvider"
            android:authorities="${applicationId}.screenlock"
            android:exported="true" />
    </application>

</manifest>
