<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws every surface, including the app bar, so the platform
      decor is switched off; this theme only covers the window before the
      first frame.
    -->
    <style name="Theme.PixelLauncherEvolved" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
